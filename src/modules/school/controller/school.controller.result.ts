export type PostSchoolResult = {
  affectedId: string;
};

export type PostNewsResult = {
  affectedId: string;
};

export type PutNewsResult = {
  affectedId: string;
};

export type DeleteNewsResult = {
  affectedId: string;
};
