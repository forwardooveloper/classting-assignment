export type AffectResult = {
  affectedId: string;
};

export type GetSchoolResult = {
  id: string;
  name: string;
  region: string;
};

export type GetNewsResult = {
  id: string;
  newsId: string;
  title: string;
  content: string;
};
